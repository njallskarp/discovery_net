__pycache__/
*.py[cod]
.coverage
.mypy_cache/
.pytest_cache/
.ruff_cache/
.venv/
build/
dist/
node_modules/
*.egg-info/
.DS_Store

# Local node identities, ledgers, and signing keys.
/run/

# Terraform runtime data. Commit only *.tfvars.example templates.
**/.terraform/
*.tfplan
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example

# Session-local handoff and follow-up notes.
*.local.md

# Generated contributor and agent signing keys.
*.pem

# Terraform plan files saved without the .tfplan extension.
tfplan

# The repository is installed with pip; uv's lockfile is a local convenience.
uv.lock

# Local agent-session scratch output.
agent-sessions/**/*.out
