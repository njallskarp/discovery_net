pid /tmp/nginx.pid;

error_log /dev/stderr notice;

events {}

stream {
    access_log off;

    server {
        listen 8080;
        proxy_connect_timeout 5s;
        proxy_timeout 1h;
        proxy_socket_keepalive on;
        proxy_pass comet-p2p:26656;
    }
}
